## forbidden product claims
code/brioweb/lib/demo/fixture.ts:217:    metric(anchorDate, "heart_rate_trend", "Heart-rate trend", today.heartRateTrendBpm, "bpm", helperStale ? "stale" : "within", helperStale ? "Stale" : "Within", "Ordinary heart-rate observations, not HRV or measured resting heart rate.", "Simulated Apple Health", "synthetic_fixture", helperStale ? "stale" : "fresh"),
code/brioweb/app/(authenticated)/recovery/page.tsx:4:export default async function RecoveryPage() { const user = await requireUser(); const [sleep, heart] = await Promise.all([getSleepSummaryForUser(user.id), getHeartRateDailyStatsForUser(user.id)]); return <div className="page-shell"><div className="page-head"><div><span className="eyebrow">Recovery</span><h1 className="page-title">Evidence bands, not a readiness score.</h1><p className="page-subtitle">Brio shows what was observed, against your own recent records, without a recovery percentage.</p></div></div><div className="stack"><div className="card"><div className="split"><div><span className="eyebrow">Sleep duration</span><h2 className="section-title">{sleep.length ? `${sleep.length} available nights` : "No consented sleep history"}</h2><p className="muted">Asleep segments only. Source overlap requires explicit handling.</p></div><span className="badge live">{sleep.length ? "Available" : "Missing"}</span></div></div><div className="card"><div className="split"><div><span className="eyebrow">Ordinary heart-rate trend</span><h2 className="section-title">{heart.length ? `${heart.length} available days` : "No consented observations"}</h2><p className="muted">This is not HRV and is not presented as measured resting heart rate.</p></div><span className="badge live">{heart.length ? "Available" : "Missing"}</span></div></div></div></div>; }
code/brioweb/app/api/decisions/live/route.ts:36:    const agent = new Agent({ name: "Brio bounded decision agent", model: DEFAULT_MODEL, outputType: CandidateSchema, instructions: "Choose exactly one allowed Brio action from the supplied consent-filtered evidence. Be conservative. Do not diagnose, invent missing evidence, claim recovery percentages, mention HRV, praise the user, or exceed the bounded change. Escalate only when a user-entered stop condition requires refusing session generation. Output only the structured schema." });
## likely secret assignments in tracked/changed files
