# P08 v1 — Turn agents into the decision and explain surface

Derived from P07: consent-filtered live metrics and durable daily inputs are available. Implement the reliable deterministic bounded policy, persisted current decision, explain card, accept/override audit, and a separate real Agents SDK route. The live route must check AI consent and credentials before execution, send only allowed current evidence, require strict structured output, and revalidate exactly Progress, Maintain, Repeat, Reduce, or Escalate. Never disguise a fallback as live.
