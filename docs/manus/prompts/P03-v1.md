# P03 v1 — Implement BRIO persistence and consent enforcement

Read P00, `docs/manus/state.md`, `docs/manus/contracts.md`, `code/brioweb/AGENTS.md`, `code/brioweb/db/schema.ts`, the current query modules, and all runtime schemas in `code/brioweb/lib/contracts/`. Work on `manus/brio-hackathon-20260912`. The local PostgreSQL database is available through ignored `code/brioweb/.env.local`; existing migrations are applied.

Extend `db/schema.ts` with compact user-owned tables for preferences/consent state, consent events or per-signal rows, daily check-ins, manual logs, training sessions, decisions, and immutable decision events. Use JSONB only for already Zod-validated nested payloads. Add mutation-ID uniqueness scoped to owner, revision columns, UTC timestamps, deletion/account state sufficient to block in-flight derived writes, and indexes required by user/date/history reads.

Fix the verified P0 isolation defect in both `health_samples` and `workouts`: replace global `external_id` uniqueness with `(user_id, external_id)`, update both upsert conflict targets, and preserve PostgreSQL behavior where multiple null external IDs append. Generate migrations only with `npm run db:generate`, inspect the generated SQL, then run `npm run db:migrate`. Do not edit historical migrations, use push, or hand-write a runner.

Create server-only query modules for each product area. Implement authenticated handlers for preferences, consent, check-ins, manual logs, training sessions, decisions, source status, export, and account deletion as documented. Every handler derives ownership from Better Auth. Use transactions for consent version changes/invalidation, session completion/projection, decision response, and deletion. Return 409 for stale revisions or duplicate terminal transitions and clear field errors for invalid input.

Update `/api/health-samples` to use the shared `HealthSampleRequestSchema`, preserve all three payload forms, enforce max 250, finite values and interval ordering, and apply per-type consent. For backward compatibility, a first helper request from an account with no product consent rows may be stored but must be marked unavailable to decision processing until explicit consent; once consent state exists, disabled health types are skipped deterministically. Return inserted/upserted samples plus accepted/skipped counts without breaking existing `{samples}` consumers. Agent/query paths must use consent-filtered accessors; generic recent-sample tools are included.

Consent updates increment one monotonic version, write timestamps immediately, mark affected proposed/current decisions stale, and prevent stale-version saves. `server_ai_processing` off must make the live agent path ineligible without issuing a model call. Keep historical audit rows but never reuse revoked evidence as current context. Worker eligibility and pre-write version recheck may be completed in this stage or explicitly blocked for P08, but do not claim enforcement unless tested.

Implement idempotent upserts for daily check-ins, manual logs, sessions, and responses using `(user_id, mutation_id)`. Session completion projects to one workout with `external_id = brio-session:<session-id>` in the same transaction. Keep all app and route imports behind `db/queries/*`; no route imports `db/client.ts` or `db/schema.ts`.

Add database-backed tests/scripts using two disposable users. Prove account B cannot overwrite/read account A through a shared external ID; repeated same-owner ingest updates one stable record; two null IDs append; invalid payloads reject; default-off consent blocks decision reads; consent change stales affected decisions; manual/check-in/session writes survive reload and do not duplicate; stale revisions return conflict. Rerun lint, TypeScript, contract tests, migrations, and production build. Update all five docs and save the exact refined P04 prompt from actual results.
