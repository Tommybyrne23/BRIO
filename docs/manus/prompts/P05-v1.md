# P05 v1 — Build the branded workspace shell and honest onboarding

Read P00, `docs/manus/state.md`, `coverage.md`, `Wireframe.md`, `brand.md`, web AGENTS rules, and the Next 16 route/PWA guidance. Work at P04 commit `0970d39` on `manus/brio-hackathon-20260912`. Preserve all P03 routes and the `lib/demo` interfaces.

Copy the supplied BRIO lockup/icon assets into `code/brioweb/public/` and implement the warm editorial workspace using the exact brand colours, cream canvas, paper cards, serif display headings, sans body type, dark teal primary actions, orange selection accents, thin blue-green borders, subtle red error, large radii, and restrained/no shadow. Do not introduce a generic dashboard kit, dark mode, praise, badges, progress confetti, or chat-first layout.

Create one responsive shell for authenticated product routes and a guest synthetic `/demo` route. Desktop uses a calm left rail; mobile uses a thumb-safe bottom bar. Exactly five primary areas appear: Today, Training, Nutrition, Recovery, Data. Chat is a secondary “Ask Brio” drawer/page and is labelled unavailable until explicit AI consent. Keep Profile, sign-out, and admin access without promoting them to the five areas. Every navigation target must resolve.

Replace the public landing with value before sign-up: show a complete synthetic recommendation card immediately, labels “Synthetic inputs” and “Simulated decision,” an explain/evidence control, and CTA hierarchy Explore demo, Create account, Sign in. Do not claim diagnostics, recovery percentage, HRV, measured resting HR, or specialist execution. Guest demo state uses `createDemoStoreState`, loads/persists with `loadDemoState`/`persistDemoState`, and has a visible reset and scenario chooser.

Change sign-up to exactly two visible fields: email and password. Generate a neutral Better Auth name/username client-side from email plus a collision-resistant suffix; never display or claim user-selected identity. Keep sign-in truthful to its current username/email support only if verified. Do not render social providers. Change forgot-password messaging so it does not claim email delivery while the backend only logs a reset URL; label it unavailable for this submission or provide a truthful local-development explanation.

Implement authenticated `/onboarding` with five skippable steps: permission priming marked “copy pending review”; signal-level consent with purpose/source, every optional toggle default off; Connect sources showing Apple helper as iPhone companion, Manual available, provider connections staged; Goal and block with safe defaults; Dietary restrictions deterministic parse preview and user confirmation; usage mode quiet/guided/coach. Save preferences and consent through the P03 APIs with loading, inline errors, retry, persisted back navigation, and completion. A declined signal still permits manual mode. Redirect incomplete authenticated accounts to onboarding from product pages without creating loops; guest demo bypasses account onboarding.

Add shell/accessibility tests where lightweight; run lint, TypeScript, contract/demo tests, and production build. Update coverage from implementation evidence and produce P06 from actual UI findings.
