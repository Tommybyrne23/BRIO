# P15 v1 — Deploy the existing architecture and prove it

Derived from P14: static and integration gates pass. Preserve the Dockerfile and Dokploy Compose service ordering; forward model/worker/demo configuration; document exact public BETTER_AUTH_URL. Build the production Next app, run it on 0.0.0.0:3000, verify local and sandbox HTTPS root/demo/manifest, and browser-test signup origin. Treat Docker/Dokploy as externally blocked if access is unavailable; do not call sandbox preview a final deployment.
