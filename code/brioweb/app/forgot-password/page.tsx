import Image from "next/image";
import Link from "next/link";

export default function ForgotPasswordPage() {
  return <main className="auth-shell"><section className="auth-art"><Link href="/"><Image src="/brio-lockup.svg" alt="Brio" width={150} height={55}/></Link><p className="auth-quote">Account recovery should be <span>truthful</span> about what is available.</p><p className="muted small">Submission environment · email delivery not configured</p></section><section className="auth-panel"><div className="auth-card"><span className="eyebrow">Password reset</span><h1>Email delivery is not connected.</h1><div className="card mist compact" style={{ marginTop: 24 }}><strong>Unavailable in this submission</strong><p className="muted small">The current backend can generate a development reset URL, but it does not send email. We will not claim that a message was delivered.</p></div><p className="page-subtitle">For the hackathon demo, use a disposable account or return to sign in. This limitation is recorded in the handoff.</p><div className="cluster" style={{ marginTop: 24 }}><Link className="button" href="/sign-in">Back to sign in</Link><Link className="button button-secondary" href="/sign-up">Create disposable account</Link></div></div></section></main>;
}
